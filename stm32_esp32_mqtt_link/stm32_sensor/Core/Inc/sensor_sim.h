#ifndef __SENSOR_SIM_H
#define __SENSOR_SIM_H

/* ── 模拟传感器数据 ──────────────────────────────────────── */
/* 后续接入真实传感器时替换函数体即可 */

float get_temp(void);
float get_humidity(void);

#endif /* __SENSOR_SIM_H */
